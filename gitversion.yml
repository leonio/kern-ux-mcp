mode: ContinuousDelivery
next-version: 0.5.0

major-version-bump-message: "^.+!:.*|^BREAKING CHANGE:"
minor-version-bump-message: "^feat:"
patch-version-bump-message: "^(fix|perf|patch):"

branches:
  main:
    regex: ^main$
    increment: Patch
    is-release-branch: true
    label: ''
